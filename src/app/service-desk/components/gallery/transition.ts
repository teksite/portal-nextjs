import { TargetAndTransition, Transition } from 'motion/react';

const transition: Transition<any> = { duration: 0.4, ease: 'circOut' };
const exit: TargetAndTransition = { transition: { duration: 1.4, ease: 'circOut' } };
export const aaa = {};
